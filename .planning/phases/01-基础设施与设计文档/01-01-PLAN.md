---
phase: 01-基础设施与设计文档
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - electron.vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - tsconfig.web.json
  - vitest.config.ts
  - src/main/index.ts
  - src/preload/index.ts
  - src/renderer/main.ts
  - src/renderer/App.vue
  - src/renderer/pages/Settings.vue
  - src/renderer/store/configStore.ts
  - src/renderer/bridge/ipcClient.ts
  - src/shared/types.ts
  - src/main/__tests__/auth.test.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03

must_haves:
  truths:
    - "npm run dev 启动后 Electron 窗口正常显示，Vue 3 渲染进程加载成功"
    - "npm run test:unit -- --run 命令可执行，测试框架 vitest 正常工作"
    - "src/main/__tests__/auth.test.ts 中的 stub 测试存在（初始为 failing/pending 状态）"
    - "preload 脚本通过 contextBridge 暴露 window.electron API，渲染进程无 nodeIntegration"
  artifacts:
    - path: "electron.vite.config.ts"
      provides: "三目标构建配置（main/preload/renderer）"
    - path: "src/main/index.ts"
      provides: "主进程入口，BrowserWindow + preload 配置"
    - path: "src/preload/index.ts"
      provides: "contextBridge 白名单 API 暴露"
    - path: "src/renderer/main.ts"
      provides: "Vue 3 + Pinia 应用初始化"
    - path: "src/main/__tests__/auth.test.ts"
      provides: "Wave 0 测试 stub，覆盖 CONF-01/02/03"
    - path: "vitest.config.ts"
      provides: "vitest 测试框架配置"
  key_links:
    - from: "src/main/index.ts"
      to: "src/preload/index.ts"
      via: "BrowserWindow webPreferences.preload"
      pattern: "preload.*index"
    - from: "src/renderer/bridge/ipcClient.ts"
      to: "window.electron"
      via: "contextBridge 暴露的 API"
      pattern: "window\\.electron"
---

<objective>
将裸 Electron 项目（main.js + index.html）升级为完整的 electron-vite + Vue 3 + TypeScript + Pinia 开发骨架，同时建立 vitest 测试框架和 Wave 0 测试 stub。

Purpose: 所有后续 Plan 的基础。没有这个骨架，鉴权逻辑和 UI 无处落地。
Output: 可运行的 Electron + Vue 3 开发环境，vitest 可执行，contextBridge 安全边界建立。
</objective>

<execution_context>
@C:/Users/shuopeng/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shuopeng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-基础设施与设计文档/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 安装依赖并重构项目骨架</name>
  <files>
    package.json,
    electron.vite.config.ts,
    tsconfig.json,
    tsconfig.node.json,
    tsconfig.web.json,
    src/main/index.ts,
    src/preload/index.ts,
    src/renderer/main.ts,
    src/renderer/App.vue,
    src/shared/types.ts
  </files>
  <behavior>
    - 安装完成后 `npm run dev` 不报错，Electron 窗口显示 Vue 渲染内容
    - `npm run build` 不报错（TypeScript 编译通过）
    - 主进程 BrowserWindow 配置了 preload，contextIsolation: true，nodeIntegration: false
  </behavior>
  <action>
1. 安装依赖（在项目根目录执行）：
```bash
npm install electron-vite --save-dev
npm install vue @vitejs/plugin-vue
npm install typescript vue-tsc --save-dev
npm install pinia
npm install @ionic/vue ionicons
npm install electron-store
npm install crypto-js jose
npm install -D @types/crypto-js
npm install vitest @vitest/coverage-v8 --save-dev
```

2. 更新 package.json scripts（保留原有 electron 依赖，新增以下 scripts）：
```json
{
  "main": "out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "test": "vitest --run",
    "test:unit": "vitest --run --reporter=verbose"
  }
}
```

3. 创建 electron.vite.config.ts（项目根目录）：
```typescript
import { defineConfig, externalizeDepsPlugin } from 'electron-vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  main: {
    plugins: [externalizeDepsPlugin({ exclude: ['jose'] })]
  },
  preload: {
    plugins: [externalizeDepsPlugin()]
  },
  renderer: {
    plugins: [vue()]
  }
})
```
注意：jose 必须从 externalize 中排除，因为它是纯 ESM，需要 Vite 打包处理 ESM→CJS 转换。

4. 创建 tsconfig.json（根目录）：
```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.node.json" },
    { "path": "./tsconfig.web.json" }
  ]
}
```

5. 创建 tsconfig.node.json（主进程 + preload）：
```json
{
  "extends": "@electron-toolkit/tsconfig/tsconfig.node.json",
  "include": ["electron.vite.config.*", "src/main/**/*", "src/preload/**/*"],
  "compilerOptions": {
    "composite": true,
    "types": ["electron-vite/node"]
  }
}
```
如果 @electron-toolkit/tsconfig 不存在，使用以下替代：
```json
{
  "compilerOptions": {
    "composite": true,
    "module": "CommonJS",
    "moduleResolution": "node",
    "target": "ES2020",
    "strict": true,
    "outDir": "out/main",
    "types": ["node"]
  },
  "include": ["electron.vite.config.*", "src/main/**/*", "src/preload/**/*"]
}
```

6. 创建 tsconfig.web.json（渲染进程）：
```json
{
  "compilerOptions": {
    "composite": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "target": "ES2020",
    "strict": true,
    "jsx": "preserve",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "types": ["vite/client"]
  },
  "include": ["src/renderer/**/*", "src/shared/**/*"]
}
```

7. 创建 src/shared/types.ts（主进程/渲染进程共享类型）：
```typescript
export interface ConfigStatus {
  jimengConfigured: boolean
  klingConfigured: boolean
}

export interface SaveConfigPayload {
  jimengAk?: string
  jimengSk?: string
  klingAk?: string
  klingSk?: string
}

export interface IpcResult<T = void> {
  success: boolean
  data?: T
  error?: string
}
```

8. 创建 src/main/index.ts（主进程入口，替换原 main.js）：
```typescript
import { app, BrowserWindow } from 'electron'
import { join } from 'path'

function createWindow(): void {
  const win = new BrowserWindow({
    width: 1200,
    height: 800,
    webPreferences: {
      preload: join(__dirname, '../preload/index.js'),
      contextIsolation: true,
      nodeIntegration: false,
      sandbox: false
    }
  })

  if (process.env['ELECTRON_RENDERER_URL']) {
    win.loadURL(process.env['ELECTRON_RENDERER_URL'])
  } else {
    win.loadFile(join(__dirname, '../renderer/index.html'))
  }
}

app.whenReady().then(() => {
  createWindow()
  app.on('activate', () => {
    if (BrowserWindow.getAllWindows().length === 0) createWindow()
  })
})

app.on('window-all-closed', () => {
  if (process.platform !== 'darwin') app.quit()
})
```

9. 创建 src/preload/index.ts（contextBridge，暴露白名单 API）：
```typescript
import { contextBridge, ipcRenderer } from 'electron'
import type { SaveConfigPayload, ConfigStatus, IpcResult } from '../shared/types'

contextBridge.exposeInMainWorld('electron', {
  saveConfig: (payload: SaveConfigPayload): Promise<IpcResult> =>
    ipcRenderer.invoke('config:save', payload),
  getConfigStatus: (): Promise<IpcResult<ConfigStatus>> =>
    ipcRenderer.invoke('config:get-status'),
  verifyJimeng: (): Promise<IpcResult> =>
    ipcRenderer.invoke('config:verify-jimeng'),
  verifyKling: (): Promise<IpcResult> =>
    ipcRenderer.invoke('config:verify-kling'),
})
```

10. 创建 src/renderer/main.ts（Vue 3 + Pinia 初始化）：
```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'

const app = createApp(App)
app.use(createPinia())
app.mount('#app')
```

11. 创建 src/renderer/App.vue（最小骨架）：
```vue
<template>
  <div id="app">
    <router-view v-if="false" />
    <Settings />
  </div>
</template>

<script setup lang="ts">
import Settings from './pages/Settings.vue'
</script>
```

12. 创建 src/renderer/index.html（渲染进程入口 HTML，放在 src/renderer/ 下）：
```html
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>灵览</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="./main.ts"></script>
  </body>
</html>
```

删除根目录的旧 main.js 和 index.html（或保留备份），因为 electron-vite 使用 src/ 目录结构。
  </action>
  <verify>
    <automated>cd D:/Code/front/linglan && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>npm run build 无 TypeScript 错误，out/ 目录生成 main/index.js、preload/index.js、renderer/index.html</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立 vitest 配置和 Wave 0 测试 stub</name>
  <files>
    vitest.config.ts,
    src/main/__tests__/auth.test.ts,
    src/renderer/pages/Settings.vue,
    src/renderer/store/configStore.ts,
    src/renderer/bridge/ipcClient.ts
  </files>
  <behavior>
    - `npm run test:unit -- --run` 执行后输出测试结果（stub 测试为 todo/skip 状态，不报错）
    - auth.test.ts 包含 hmacSigner、jwtSigner、configStore 三个 describe 块的 stub
    - Settings.vue 渲染后显示即梦和可灵两个配置区块
    - configStore.ts 只存储布尔状态（jimengConfigured/klingConfigured），不存储明文密钥
  </behavior>
  <action>
1. 创建 vitest.config.ts（项目根目录）：
```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/main/**/*.test.ts'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json'],
      include: ['src/main/**/*.ts'],
      exclude: ['src/main/__tests__/**', 'src/main/index.ts']
    }
  }
})
```

2. 创建 src/main/__tests__/auth.test.ts（Wave 0 stub，为后续 Plan 03 的实现预留）：
```typescript
import { describe, it, expect } from 'vitest'

// Wave 0: 测试 stub — 这些测试在 Plan 03 实现鉴权逻辑后会变为真实测试
// 当前状态：todo（占位），确保测试框架可运行

describe('hmacSigner (即梦 HMAC-SHA256 V4)', () => {
  it.todo('buildJimengAuthHeaders 返回包含 Authorization、X-Date、X-Content-Sha256 的对象')
  it.todo('Authorization header 格式为 HMAC-SHA256 Credential=.../Signature=...')
  it.todo('X-Date 格式为 YYYYMMDDTHHmmssZ（15位）')
  it.todo('相同输入产生相同签名（确定性）')
})

describe('jwtSigner (可灵 JWT HS256)', () => {
  it.todo('getKlingToken 返回有效的 JWT 字符串（三段 base64url）')
  it.todo('JWT payload 包含 iss=ak、exp=now+1800、nbf=now-5')
  it.todo('token 在有效期内（exp-now > 60s）时复用缓存，不重新生成')
  it.todo('invalidateKlingToken 后下次调用重新生成 token')
})

describe('configStore (密钥存储)', () => {
  it.todo('saveCredentials 存储后 hasCredentials 返回 true')
  it.todo('getCredentials 返回解密后的原始字符串')
  it.todo('getConfigStatus 只返回布尔值，不返回明文密钥')
})
```

3. 创建 src/renderer/store/configStore.ts（渲染进程配置状态，只存布尔值）：
```typescript
import { defineStore } from 'pinia'
import { ref } from 'vue'
import type { ConfigStatus } from '../../shared/types'

export const useConfigStore = defineStore('config', () => {
  const jimengConfigured = ref(false)
  const klingConfigured = ref(false)
  const loading = ref(false)

  async function refreshStatus(): Promise<void> {
    loading.value = true
    try {
      const result = await window.electron.getConfigStatus()
      if (result.success && result.data) {
        jimengConfigured.value = result.data.jimengConfigured
        klingConfigured.value = result.data.klingConfigured
      }
    } finally {
      loading.value = false
    }
  }

  return { jimengConfigured, klingConfigured, loading, refreshStatus }
})
```

4. 创建 src/renderer/bridge/ipcClient.ts（window.electron 类型声明）：
```typescript
import type { SaveConfigPayload, ConfigStatus, IpcResult } from '../../shared/types'

// 扩展 Window 接口，提供 window.electron 类型
declare global {
  interface Window {
    electron: {
      saveConfig(payload: SaveConfigPayload): Promise<IpcResult>
      getConfigStatus(): Promise<IpcResult<ConfigStatus>>
      verifyJimeng(): Promise<IpcResult>
      verifyKling(): Promise<IpcResult>
    }
  }
}

export {}
```

5. 创建 src/renderer/pages/Settings.vue（设置页骨架，CONF-01/02/03 的 UI 占位）：
```vue
<template>
  <div class="settings-page">
    <h1>API 配置</h1>

    <!-- 即梦配置区块 (CONF-01) -->
    <section class="config-section">
      <h2>即梦 (Jimeng)</h2>
      <div class="status-badge" :class="{ configured: configStore.jimengConfigured }">
        {{ configStore.jimengConfigured ? '已配置' : '未配置' }}
      </div>
      <div class="input-group">
        <input v-model="jimengAk" type="password" placeholder="Access Key (AK)" />
        <input v-model="jimengSk" type="password" placeholder="Secret Key (SK)" />
      </div>
      <button @click="saveJimeng" :disabled="!jimengAk || !jimengSk">保存即梦密钥</button>
    </section>

    <!-- 可灵配置区块 (CONF-02) -->
    <section class="config-section">
      <h2>可灵 (Kling)</h2>
      <div class="status-badge" :class="{ configured: configStore.klingConfigured }">
        {{ configStore.klingConfigured ? '已配置' : '未配置' }}
      </div>
      <div class="input-group">
        <input v-model="klingAk" type="password" placeholder="Access Key (AK)" />
        <input v-model="klingSk" type="password" placeholder="Secret Key (SK)" />
      </div>
      <button @click="saveKling" :disabled="!klingAk || !klingSk">保存可灵密钥</button>
    </section>

    <p v-if="message" class="message">{{ message }}</p>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { useConfigStore } from '../store/configStore'

const configStore = useConfigStore()
const jimengAk = ref('')
const jimengSk = ref('')
const klingAk = ref('')
const klingSk = ref('')
const message = ref('')

onMounted(() => configStore.refreshStatus())

async function saveJimeng() {
  const result = await window.electron.saveConfig({
    jimengAk: jimengAk.value,
    jimengSk: jimengSk.value
  })
  message.value = result.success ? '即梦密钥已保存' : `保存失败: ${result.error}`
  if (result.success) {
    jimengAk.value = ''
    jimengSk.value = ''
    await configStore.refreshStatus()
  }
}

async function saveKling() {
  const result = await window.electron.saveConfig({
    klingAk: klingAk.value,
    klingSk: klingSk.value
  })
  message.value = result.success ? '可灵密钥已保存' : `保存失败: ${result.error}`
  if (result.success) {
    klingAk.value = ''
    klingSk.value = ''
    await configStore.refreshStatus()
  }
}
</script>
```
  </action>
  <verify>
    <automated>cd D:/Code/front/linglan && npm run test:unit -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>npm run test:unit -- --run 输出显示 auth.test.ts 中的 todo 测试，无报错退出（exit code 0）</done>
</task>

</tasks>

<verification>
1. `npm run build` 成功，out/ 目录包含三个构建目标
2. `npm run test:unit -- --run` 可执行，vitest 正常工作
3. src/main/__tests__/auth.test.ts 存在，包含 hmacSigner/jwtSigner/configStore 三个 describe 块
4. src/preload/index.ts 使用 contextBridge，不开启 nodeIntegration
5. src/renderer/store/configStore.ts 只存储布尔状态，无明文密钥
</verification>

<success_criteria>
- electron-vite 三目标构建（main/preload/renderer）无错误
- vitest 测试框架可运行，Wave 0 stub 存在
- contextBridge 安全边界建立，window.electron API 类型完整
- Settings.vue 包含即梦和可灵两个配置区块（UI 骨架）
</success_criteria>

<output>
完成后创建 .planning/phases/01-基础设施与设计文档/01-01-SUMMARY.md
</output>
