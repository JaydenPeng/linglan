---
phase: 03-视频生成与体验完善
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/stores/historyStore.ts
  - src/renderer/stores/promptTemplateStore.ts
  - src/shared/types/history.ts
  - src/shared/types/promptTemplate.ts
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
  - HIST-04
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03

must_haves:
  truths:
    - "历史记录在应用重启后仍然存在（localStorage 持久化）"
    - "历史记录支持图片和视频混合存储，按时间倒序排列"
    - "用户可以收藏、删除历史记录，可以复用提示词"
    - "内置提示词模板按分类组织，自定义模板可增删改"
  artifacts:
    - path: "src/shared/types/history.ts"
      provides: "历史记录类型定义"
      exports: ["HistoryRecord", "HistoryRecordType"]
    - path: "src/shared/types/promptTemplate.ts"
      provides: "提示词模板类型定义"
      exports: ["PromptTemplate", "PromptCategory"]
    - path: "src/renderer/stores/historyStore.ts"
      provides: "历史记录 Pinia store（持久化）"
      exports: ["useHistoryStore"]
    - path: "src/renderer/stores/promptTemplateStore.ts"
      provides: "提示词模板 Pinia store（持久化）"
      exports: ["usePromptTemplateStore"]
  key_links:
    - from: "src/renderer/stores/historyStore.ts"
      to: "localStorage"
      via: "pinia-plugin-persistedstate"
    - from: "src/renderer/stores/promptTemplateStore.ts"
      to: "localStorage"
      via: "pinia-plugin-persistedstate"
---

<objective>
实现历史记录和提示词模板的数据层：类型定义 + Pinia store（含 localStorage 持久化）+ 内置模板数据。

Purpose: 为 Plan 04 的 UI 组件提供完整的数据契约，UI 层只需调用 store actions，无需关心持久化细节。
Output: history.ts、promptTemplate.ts 类型文件，historyStore.ts、promptTemplateStore.ts store 文件
</objective>

<execution_context>
@C:/Users/shuopeng/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shuopeng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-视频生成与体验完善/03-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义历史记录和提示词模板类型</name>
  <files>src/shared/types/history.ts, src/shared/types/promptTemplate.ts</files>
  <behavior>
    - HistoryRecord 包含 id、type("image"|"video")、prompt、result_url、cover_url?、status、created_at、is_favorite
    - PromptTemplate 包含 id、title、content、category、is_builtin、created_at
    - PromptCategory 枚举包含: "风景" | "人物" | "动漫" | "建筑" | "抽象" | "自定义"
  </behavior>
  <action>
    1. 创建 src/shared/types/history.ts:
       - HistoryRecordType = "image" | "video"
       - HistoryRecord: { id: string, type: HistoryRecordType, prompt: string,
           result_url: string, cover_url?: string,
           status: "succeed" | "failed", created_at: number, is_favorite: boolean }

    2. 创建 src/shared/types/promptTemplate.ts:
       - PromptCategory = "风景" | "人物" | "动漫" | "建筑" | "抽象" | "自定义"
       - PromptTemplate: { id: string, title: string, content: string,
           category: PromptCategory, is_builtin: boolean, created_at: number }
       - BUILTIN_TEMPLATES: PromptTemplate[] — 内置 15 条模板，覆盖全部 5 个非自定义分类（每类 3 条）
         示例: { id:"bt-001", title:"金色草原日落", content:"金色草原，夕阳西下，光线柔和...", category:"风景", is_builtin:true, created_at:0 }
  </action>
  <verify>
    <automated>npx vitest run src/shared/types/history.test.ts src/shared/types/promptTemplate.test.ts</automated>
  </verify>
  <done>类型文件导出正确，BUILTIN_TEMPLATES 包含 15 条内置模板，覆盖 5 个分类</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 historyStore 和 promptTemplateStore</name>
  <files>src/renderer/stores/historyStore.ts, src/renderer/stores/promptTemplateStore.ts</files>
  <behavior>
    historyStore:
    - addRecord(record) 将记录插入列表头部
    - toggleFavorite(id) 切换 is_favorite
    - deleteRecord(id) 从列表移除
    - getPromptById(id) 返回对应记录的 prompt 字符串（用于复用提示词 HIST-02）
    - filteredRecords(filter: "all"|"favorite") 返回过滤后的列表

    promptTemplateStore:
    - templates 计算属性：内置模板 + 自定义模板合并
    - addCustomTemplate(title, content, category) 创建并保存自定义模板
    - deleteCustomTemplate(id) 仅允许删除非内置模板
    - getByCategory(category) 返回该分类的模板列表
  </behavior>
  <action>
    1. 创建 src/renderer/stores/historyStore.ts（Pinia defineStore）:
       - state: { records: HistoryRecord[] }
       - persist: true（pinia-plugin-persistedstate，key: "linglan-history"）
       - 实现上述 actions 和 getters

    2. 创建 src/renderer/stores/promptTemplateStore.ts（Pinia defineStore）:
       - state: { customTemplates: PromptTemplate[] }
       - persist: true（key: "linglan-prompt-templates"）
       - computed templates = [...BUILTIN_TEMPLATES, ...customTemplates]
       - 实现上述 actions 和 getters

    两个 store 均使用 Pinia Composition API 风格（与 Phase 2 保持一致）。
  </action>
  <verify>
    <automated>npx vitest run src/renderer/stores/historyStore.test.ts src/renderer/stores/promptTemplateStore.test.ts</automated>
  </verify>
  <done>store actions 测试全部通过，持久化配置正确，deleteCustomTemplate 拒绝删除内置模板</done>
</task>

</tasks>

<verification>
- src/shared/types/history.ts 和 promptTemplate.ts 导出正确
- BUILTIN_TEMPLATES 包含 15 条，覆盖 5 个分类
- historyStore 和 promptTemplateStore 单元测试通过
- persist 配置使用正确的 localStorage key
</verification>

<success_criteria>
数据层就绪：历史记录和提示词模板的类型、store、内置数据全部完成，Plan 04 的 UI 组件可直接调用 store actions。
</success_criteria>

<output>
完成后创建 .planning/phases/03-视频生成与体验完善/03-02-SUMMARY.md
</output>
