---
phase: 03-视频生成与体验完善
plan: "04"
type: execute
wave: 2
depends_on:
  - "03-02"
files_modified:
  - src/renderer/views/HistoryPage.vue
  - src/renderer/components/HistoryItem.vue
  - src/renderer/components/PromptTemplatePanel.vue
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
  - HIST-04
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03

must_haves:
  truths:
    - "历史记录列表图片和视频混合显示，按时间倒序"
    - "左滑历史记录条目展开操作按钮（复用提示词、收藏、删除）"
    - "顶部全部/已收藏切换按钮正确过滤列表"
    - "提示词模板面板从底部半屏弹出，支持分类标签切换"
    - "点击模板直接填入提示词输入框，面板自动关闭"
    - "我的模板 Tab 支持查看、删除自定义模板，并可新增"
  artifacts:
    - path: "src/renderer/views/HistoryPage.vue"
      provides: "历史记录页面（全部/收藏筛选 + 列表）"
    - path: "src/renderer/components/HistoryItem.vue"
      provides: "历史记录条目（IonItemSliding 左滑操作）"
    - path: "src/renderer/components/PromptTemplatePanel.vue"
      provides: "提示词模板底部面板（分类浏览 + 我的模板）"
  key_links:
    - from: "src/renderer/components/HistoryItem.vue"
      to: "src/renderer/stores/historyStore.ts"
      via: "toggleFavorite / deleteRecord / getPromptById"
    - from: "src/renderer/components/PromptTemplatePanel.vue"
      to: "src/renderer/stores/promptTemplateStore.ts"
      via: "getByCategory / addCustomTemplate / deleteCustomTemplate"
    - from: "src/renderer/components/PromptTemplatePanel.vue"
      to: "VideoGeneratePage / ImageGeneratePage 提示词输入框"
      via: "emit: select(content: string)"
---

<objective>
实现历史记录页面和提示词模板面板 UI，接入 Plan 02 的 store 数据层。

Purpose: 用户可以查看历史、复用提示词、收藏删除记录，并通过模板库快速填入提示词。
Output: HistoryPage、HistoryItem、PromptTemplatePanel 组件
</objective>

<execution_context>
@C:/Users/shuopeng/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shuopeng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-视频生成与体验完善/03-CONTEXT.md
@.planning/phases/03-视频生成与体验完善/03-02-SUMMARY.md

<interfaces>
<!-- 来自 Plan 02 产出 -->
From src/renderer/stores/historyStore.ts:
  useHistoryStore() → {
    records: HistoryRecord[],
    filteredRecords(filter: "all"|"favorite"): HistoryRecord[],
    addRecord(record): void,
    toggleFavorite(id): void,
    deleteRecord(id): void,
    getPromptById(id): string
  }

From src/renderer/stores/promptTemplateStore.ts:
  usePromptTemplateStore() → {
    templates: PromptTemplate[],
    getByCategory(category): PromptTemplate[],
    addCustomTemplate(title, content, category): void,
    deleteCustomTemplate(id): void
  }

From src/shared/types/history.ts:
  HistoryRecord: { id, type, prompt, result_url, cover_url?, status, created_at, is_favorite }

From src/shared/types/promptTemplate.ts:
  PromptTemplate: { id, title, content, category, is_builtin, created_at }
  PromptCategory = "风景"|"人物"|"动漫"|"建筑"|"抽象"|"自定义"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HistoryItem 组件 + HistoryPage 页面</name>
  <files>src/renderer/components/HistoryItem.vue, src/renderer/views/HistoryPage.vue</files>
  <action>
    1. 创建 src/renderer/components/HistoryItem.vue:
       - 使用 IonItemSliding 包裹，左滑展开 IonItemOptions:
         - "复用提示词" 按钮（color="primary"）：emit reuse-prompt(prompt: string)
         - "收藏/取消收藏" 按钮（color="warning"）：调用 historyStore.toggleFavorite(id)
         - "删除" 按钮（color="danger"）：调用 historyStore.deleteRecord(id)，需 IonAlert 二次确认
       - 条目内容：左侧 56x56 缩略图（图片直接显示，视频显示 cover_url 或视频图标占位）
         右侧：提示词预览（最多 2 行截断）、时间（相对时间，如"3分钟前"）、类型 badge（图片/视频）
       - is_favorite=true 时显示收藏星标图标

    2. 创建 src/renderer/views/HistoryPage.vue:
       - 顶部 IonSegment：全部 / 已收藏 切换，切换时调用 filteredRecords()
       - 列表：v-for HistoryItem，空状态显示"暂无记录"提示
       - 监听 HistoryItem 的 reuse-prompt 事件：将 prompt 存入 Pinia 临时状态或 router query，
         跳转到图片/视频生成页面并填入提示词（使用 useRouter().push）
       - 导航接入：在 Tab Bar 添加"历史"Tab（time-outline 图标），路由 /history → HistoryPage
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit src/renderer/components/HistoryItem.vue src/renderer/views/HistoryPage.vue</automated>
  </verify>
  <done>HistoryPage 和 HistoryItem 无 TypeScript 错误，历史 Tab 已加入导航</done>
</task>

<task type="auto">
  <name>Task 2: PromptTemplatePanel 底部面板组件</name>
  <files>src/renderer/components/PromptTemplatePanel.vue</files>
  <action>
    创建 src/renderer/components/PromptTemplatePanel.vue:

    - 使用 IonModal 的 breakpoints=[0, 0.5, 1] initialBreakpoint=0.5 实现底部半屏面板
    - props: { isOpen: boolean }，emit: { close(), select(content: string) }

    面板内部结构:
    1. 顶部 IonSegment 两个 Tab：
       - "模板库" Tab：按分类浏览内置 + 自定义模板
       - "我的模板" Tab：仅显示自定义模板，支持管理

    2. "模板库" Tab:
       - 顶部横向分类标签（IonChip 列表，可横向滚动）：风景/人物/动漫/建筑/抽象/自定义
       - 选中分类后显示该分类模板卡片列表（IonCard）
       - 每张卡片：标题 + 内容预览（2行截断）
       - 点击卡片：emit select(template.content)，面板自动关闭（emit close()）

    3. "我的模板" Tab:
       - 列表显示自定义模板（is_builtin=false）
       - 每条右侧：删除按钮（IonAlert 二次确认后调用 deleteCustomTemplate）
       - 底部"新增模板"按钮：展开内联表单（标题输入 + 内容输入 + 分类选择 + 保存按钮）
         保存调用 addCustomTemplate，表单清空收起

    接入：在 VideoGeneratePage 和图片生成页面的提示词输入框下方添加
    "从模板选择" IonButton（outline 样式），点击设置 isOpen=true 展开面板，
    监听 select 事件填入提示词输入框。
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit src/renderer/components/PromptTemplatePanel.vue</automated>
  </verify>
  <done>PromptTemplatePanel 无 TypeScript 错误，isOpen/close/select 接口正确，两个 Tab 结构完整</done>
</task>

</tasks>

<verification>
- 历史记录页面可通过 Tab 导航访问
- 左滑操作展开三个按钮（复用/收藏/删除）
- 全部/已收藏切换正确过滤
- 提示词模板面板从底部半屏弹出
- 分类标签切换显示对应模板
- 点击模板填入提示词并关闭面板
- 我的模板 Tab 支持新增和删除
</verification>

<success_criteria>
历史记录和提示词模板 UI 完整可用：用户可查看历史、左滑操作、收藏筛选，可从模板库选择模板填入提示词，可管理自定义模板。
</success_criteria>

<output>
完成后创建 .planning/phases/03-视频生成与体验完善/03-04-SUMMARY.md
</output>
