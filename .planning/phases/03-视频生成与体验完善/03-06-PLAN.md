---
phase: 03-视频生成与体验完善
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - src/renderer/pages/ImageGeneratePage.vue
autonomous: true
gap_closure: true
deferred: true
requirements:
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03

must_haves:
  truths:
    - "ImageGeneratePage 存在后，PromptTemplatePanel 接入图片生成页面，点击模板填入提示词"
  artifacts:
    - path: "src/renderer/pages/ImageGeneratePage.vue"
      provides: "图片生成页面，包含提示词输入框和'从模板选择'按钮"
  key_links:
    - from: "src/renderer/pages/ImageGeneratePage.vue"
      to: "src/renderer/components/PromptTemplatePanel.vue"
      via: "emit select(content)"
      pattern: "onTemplateSelect"
---

<objective>
将 PromptTemplatePanel 接入 ImageGeneratePage（PROMPT-01/02/03 对图片生成的覆盖）。

Purpose: Phase 3 验证发现 ImageGeneratePage 不存在（Phase 2 未完成该页面），导致 PromptTemplatePanel 无法接入图片生成流程。本计划在 Phase 2 补全 ImageGeneratePage 后执行。

Output: ImageGeneratePage.vue 中集成 PromptTemplatePanel，与 VideoGeneratePage 的接入方式保持一致。

BLOCKED: 此计划依赖 Phase 2 完成 ImageGeneratePage。当前 src/renderer/pages/ 中不存在该文件。
执行前提：Phase 2 gap closure 完成 ImageGeneratePage 实现，并在 App.vue 中注册图片生成 Tab。
</objective>

<execution_context>
@C:/Users/shuopeng/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shuopeng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- VideoGeneratePage 的接入方式作为参考模板 -->
<!-- src/renderer/pages/VideoGeneratePage.vue（已验证可用） -->
```vue
<!-- 模板部分 -->
<button class="template-btn" type="button" @click="showTemplatePanel = true">
  从模板选择
</button>
<PromptTemplatePanel
  v-if="showTemplatePanel"
  @select="onTemplateSelect"
  @close="showTemplatePanel = false"
/>

<!-- script 部分 -->
import PromptTemplatePanel from '../components/PromptTemplatePanel.vue'
const showTemplatePanel = ref(false)
function onTemplateSelect(content: string) {
  prompt.value = content
  showTemplatePanel.value = false
}
```

<!-- PromptTemplatePanel emit 接口 -->
<!-- src/renderer/components/PromptTemplatePanel.vue -->
```typescript
// emits: ['select', 'close']
// select(content: string) — 用户点击模板时触发，content 为模板提示词
// close() — 用户点击遮罩或关闭按钮时触发
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 确认 ImageGeneratePage 存在并接入 PromptTemplatePanel</name>
  <files>src/renderer/pages/ImageGeneratePage.vue</files>
  <action>
    前提检查：确认 ImageGeneratePage.vue 已由 Phase 2 gap closure 创建。

    在 ImageGeneratePage.vue 中接入 PromptTemplatePanel，完全复用 VideoGeneratePage 的接入模式：

    1. import PromptTemplatePanel from '../components/PromptTemplatePanel.vue'
    2. 新增 ref：const showTemplatePanel = ref(false)
    3. 在提示词输入框下方添加"从模板选择"按钮：
       @click="showTemplatePanel = true"
    4. 在模板末尾（</div> 前）添加：
       ```vue
       <PromptTemplatePanel
         v-if="showTemplatePanel"
         @select="onTemplateSelect"
         @close="showTemplatePanel = false"
       />
       ```
    5. 添加处理函数：
       ```typescript
       function onTemplateSelect(content: string) {
         prompt.value = content
         showTemplatePanel.value = false
       }
       ```

    注意：ImageGeneratePage 的提示词 ref 名称以实际实现为准（可能是 prompt、promptText 等），
    执行时先检查现有变量名再接入。
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json 2>&1 | head -20</automated>
  </verify>
  <done>
    ImageGeneratePage 包含"从模板选择"按钮，点击展开 PromptTemplatePanel，
    选择模板后提示词填入输入框，面板关闭。TypeScript 编译无错误。
  </done>
</task>

</tasks>

<verification>
npx tsc --noEmit -p tsconfig.web.json

预期：0 错误。
验证链路：ImageGeneratePage "从模板选择" → showTemplatePanel=true → PromptTemplatePanel 渲染 → emit select(content) → onTemplateSelect → prompt.value 填入 → showTemplatePanel=false。
</verification>

<success_criteria>
- ImageGeneratePage 存在（Phase 2 gap closure 前提）
- PromptTemplatePanel 接入图片生成页面，行为与视频生成页面一致
- PROMPT-01/02/03 对图片生成的覆盖完成
- TypeScript 编译无错误
</success_criteria>

<output>
完成后创建 .planning/phases/03-视频生成与体验完善/03-06-SUMMARY.md
</output>
