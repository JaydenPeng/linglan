---
phase: 03-视频生成与体验完善
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/store/uiStore.ts
  - src/renderer/App.vue
  - src/renderer/pages/HistoryPage.vue
autonomous: true
gap_closure: true
requirements:
  - HIST-02

must_haves:
  truths:
    - "用户点击历史记录中的'复用提示词'后，自动跳转到视频生成 Tab，提示词已填入输入框"
  artifacts:
    - path: "src/renderer/store/uiStore.ts"
      provides: "共享 activeTab 状态，供 App.vue 和 HistoryPage 共同读写"
      exports: ["useUiStore"]
    - path: "src/renderer/App.vue"
      provides: "从 uiStore 读取 activeTab，替换本地 ref"
    - path: "src/renderer/pages/HistoryPage.vue"
      provides: "onReusePrompt 写入 sessionStorage 后调用 uiStore 切换 Tab"
  key_links:
    - from: "src/renderer/pages/HistoryPage.vue"
      to: "src/renderer/App.vue"
      via: "uiStore.activeTab = 'video'"
      pattern: "uiStore\\.activeTab"
---

<objective>
修复 HIST-02 gap：用户从历史记录复用提示词后，应自动跳转到视频生成页面，而不是仅显示 toast 提示。

Purpose: 完成"复用提示词"的完整交互闭环，用户无需手动切换 Tab。
Output: uiStore.ts（共享导航状态）+ App.vue 和 HistoryPage.vue 的对应修改。
</objective>

<execution_context>
@C:/Users/shuopeng/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shuopeng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- 当前 App.vue activeTab 实现（需迁移到 uiStore） -->
<!-- src/renderer/App.vue -->
```typescript
// 当前：本地 ref，HistoryPage 无法访问
const activeTab = ref<'settings' | 'video' | 'create' | 'tasks' | 'history'>('video')
```

<!-- src/renderer/pages/HistoryPage.vue -->
```typescript
// 当前：只写 sessionStorage，不切换 Tab
function onReusePrompt(prompt: string) {
  sessionStorage.setItem('linglan-reuse-prompt', prompt)
  reuseToast.value = true
  setTimeout(() => { reuseToast.value = false }, 2500)
}
```

<!-- VideoGeneratePage.vue onMounted 已正确读取 sessionStorage -->
```typescript
// 已存在，无需修改
onMounted(() => {
  const reuse = sessionStorage.getItem('linglan-reuse-prompt')
  if (reuse) {
    prompt.value = reuse
    sessionStorage.removeItem('linglan-reuse-prompt')
  }
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 uiStore，迁移 activeTab 状态</name>
  <files>src/renderer/store/uiStore.ts</files>
  <action>
    创建 Pinia store，管理全局 UI 导航状态：

    ```typescript
    import { defineStore } from 'pinia'
    import { ref } from 'vue'

    export type TabId = 'video' | 'create' | 'tasks' | 'history' | 'settings'

    export const useUiStore = defineStore('ui', () => {
      const activeTab = ref<TabId>('video')
      function switchTab(tab: TabId) {
        activeTab.value = tab
      }
      return { activeTab, switchTab }
    })
    ```

    不需要 persist（导航状态无需跨会话保留）。
    store 目录沿用 src/renderer/store/（单数），与 configStore.ts 保持一致。
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json 2>&1 | head -20</automated>
  </verify>
  <done>uiStore.ts 存在，导出 useUiStore，TabId 类型覆盖全部 5 个 Tab</done>
</task>

<task type="auto">
  <name>Task 2: App.vue 改用 uiStore.activeTab</name>
  <files>src/renderer/App.vue</files>
  <action>
    将 App.vue 中的本地 `activeTab` ref 替换为 uiStore：

    1. 删除 `import { ref } from 'vue'` 中的 ref（若无其他用途则整行删除）
    2. 新增 `import { useUiStore } from './store/uiStore'`
    3. 将 `const activeTab = ref<...>('video')` 替换为：
       ```typescript
       const uiStore = useUiStore()
       const { activeTab } = storeToRefs(uiStore)
       ```
       同时 import `storeToRefs` from `'pinia'`
    4. `onNavigate` 函数改为调用 `uiStore.switchTab(tab as TabId)`
    5. 删除本地 `type TabId` 定义，改从 uiStore import

    模板中所有 `activeTab` 引用保持不变（storeToRefs 保持响应性）。
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json 2>&1 | head -20</automated>
  </verify>
  <done>App.vue 编译无错误，activeTab 来自 uiStore，Tab 切换功能不变</done>
</task>

<task type="auto">
  <name>Task 3: HistoryPage.onReusePrompt 写入后自动切换 Tab</name>
  <files>src/renderer/pages/HistoryPage.vue</files>
  <action>
    修改 `onReusePrompt`，在写入 sessionStorage 后立即切换到视频 Tab：

    1. 新增 import：`import { useUiStore } from '../store/uiStore'`
    2. 在 script setup 中初始化：`const uiStore = useUiStore()`
    3. 修改 `onReusePrompt`：
       ```typescript
       function onReusePrompt(prompt: string) {
         sessionStorage.setItem('linglan-reuse-prompt', prompt)
         uiStore.switchTab('video')
         // 保留 toast 作为视觉反馈（会在视频页显示约 2.5s）
         reuseToast.value = true
         setTimeout(() => { reuseToast.value = false }, 2500)
       }
       ```

    注意：Tab 切换后 HistoryPage 会被卸载，toast 实际上不会显示。
    可以将 toast 逻辑移除，或保留（无害，只是不可见）。
    推荐：移除 reuseToast ref 和模板中的 toast div，保持代码整洁。
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json 2>&1 | head -20</automated>
  </verify>
  <done>
    点击"复用提示词"后：
    1. sessionStorage 写入提示词
    2. activeTab 切换到 'video'
    3. VideoGeneratePage onMounted 读取并清除 sessionStorage，提示词填入输入框
    编译无 TS 错误
  </done>
</task>

</tasks>

<verification>
npx tsc --noEmit -p tsconfig.web.json

预期：0 错误。
验证链路：HistoryPage.onReusePrompt → uiStore.switchTab('video') → App.vue 渲染 VideoGeneratePage → onMounted 读取 sessionStorage → prompt.value 填入。
</verification>

<success_criteria>
- uiStore.ts 存在，TabId 类型正确
- App.vue activeTab 来自 uiStore，行为与修改前一致
- HistoryPage 复用提示词后自动跳转到视频生成页
- TypeScript 编译无错误
- HIST-02 requirement 满足
</success_criteria>

<output>
完成后创建 .planning/phases/03-视频生成与体验完善/03-05-SUMMARY.md
</output>
