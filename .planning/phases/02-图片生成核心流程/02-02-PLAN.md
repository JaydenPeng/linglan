---
phase: 02-图片生成核心流程
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/main/imageHandler.ts
  - src/main/index.ts
autonomous: true
requirements:
  - TASK-01
  - TASK-02
  - TASK-03
  - TASK-04

must_haves:
  truths:
    - "主进程接收 image:submit IPC 调用后，调用即梦 API 提交任务并返回 jobId"
    - "主进程对每个活跃任务启动独立轮询，每 3 秒查询一次状态，通过 image:status-update 推送给渲染进程"
    - "多个任务可以同时轮询，互不干扰"
    - "收到 image:cancel 后立即停止对应任务的轮询"
    - "失败任务可以通过重新调用 image:submit（携带相同参数）重试"
  artifacts:
    - path: "src/main/imageHandler.ts"
      provides: "即梦 API 调用 + 轮询管理器"
      exports: ["registerImageHandlers"]
    - path: "src/main/index.ts"
      provides: "注册 imageHandler IPC 处理器"
  key_links:
    - from: "src/main/imageHandler.ts"
      to: "即梦 CVSync2AsyncSubmitTask API"
      via: "HMAC-SHA256 签名（来自 Phase 1 hmacSigner）"
      pattern: "hmacSigner|CVSync2AsyncSubmitTask"
    - from: "src/main/imageHandler.ts"
      to: "src/types/image.ts"
      via: "IPC_CHANNELS, ImageParams"
      pattern: "import.*IPC_CHANNELS.*from.*types/image"
    - from: "src/main/imageHandler.ts"
      to: "渲染进程 BrowserWindow"
      via: "webContents.send(IPC_CHANNELS.IMAGE_STATUS_UPDATE)"
      pattern: "webContents\\.send"
---

<objective>
实现主进程的即梦 API 调用逻辑和任务轮询管理器，处理任务提交、状态轮询、取消和状态推送。

Purpose: 主进程是唯一持有 API 密钥的地方，所有网络请求必须在此执行。
Output: src/main/imageHandler.ts（核心逻辑），注册到 src/main/index.ts
</objective>

<execution_context>
@C:/Users/shuopeng/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shuopeng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-图片生成核心流程/02-CONTEXT.md
@.planning/phases/02-图片生成核心流程/02-01-SUMMARY.md

<interfaces>
<!-- 来自 Plan 01 定义的合约 -->

From src/types/image.ts:
```typescript
export enum TaskStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  SUCCESS = 'success',
  FAILED = 'failed',
  CANCELLED = 'cancelled'
}

export interface ImageParams {
  prompt: string
  width?: number
  height?: number
  aspect_ratio?: string
  force_single?: boolean
  ref_image?: string      // base64，图生图时使用
}

export interface ImageTask {
  id: string
  jobId?: string
  type: TaskType
  status: TaskStatus
  params: ImageParams
  createdAt: number
  updatedAt: number
  resultUrls?: string[]
  errorMsg?: string
}

export const IPC_CHANNELS = {
  IMAGE_SUBMIT: 'image:submit',
  IMAGE_CANCEL: 'image:cancel',
  IMAGE_STATUS_UPDATE: 'image:status-update',
} as const
```

Phase 1 提供（假设已实现）：
```typescript
// src/main/hmacSigner.ts
export function signRequest(method: string, path: string, body: string, ak: string, sk: string): Record<string, string>

// src/main/configStore.ts
export function getConfig(): { jiMengAk: string; jiMengSk: string }
```
</interfaces>

即梦 API 关键信息（来自 CONTEXT.md）：
- 提交任务：POST /v1/images/generations（CVSync2AsyncSubmitTask）
- 查询状态：GET /v1/images/generations/{job_id}（CVSync2AsyncGetResult）
- return_url 参数必须设置为 true，否则返回 base64 数据
- 轮询间隔：3 秒，最大等待：10 分钟（200 次）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现即梦 API 调用函数</name>
  <files>src/main/imageHandler.ts</files>
  <action>
创建 src/main/imageHandler.ts，实现两个核心 API 调用函数和轮询管理器：

```typescript
import { ipcMain, BrowserWindow } from 'electron'
import { net } from 'electron'  // 使用 Electron net 模块发起请求
import type { ImageParams } from '../../src/types/image'
import { IPC_CHANNELS, TaskStatus } from '../../src/types/image'
import { signRequest } from './hmacSigner'
import { getConfig } from './configStore'

const JIMENG_BASE = 'https://visual.volcengineapi.com'
const POLL_INTERVAL_MS = 3000
const MAX_POLL_COUNT = 200  // 10 分钟上限

// 活跃轮询 Map：localId -> intervalId
const activePollers = new Map<string, ReturnType<typeof setInterval>>()

// 提交图片生成任务
async function submitImageTask(params: ImageParams): Promise<string> {
  const { jiMengAk, jiMengSk } = getConfig()
  const body = JSON.stringify({
    req_key: params.ref_image ? 'img2img_xl_sft' : 'high_aes_general_v21_L',
    prompt: params.prompt,
    width: params.width ?? 1024,
    height: params.height ?? 1024,
    return_url: true,  // 必须为 true，否则返回 base64
    ...(params.force_single && { use_sr: false, ddim_steps: 25 }),
    ...(params.ref_image && { binary_data_base64: [params.ref_image] }),
  })
  const path = '/?Action=CVSync2AsyncSubmitTask&Version=2022-08-31'
  const headers = signRequest('POST', path, body, jiMengAk, jiMengSk)

  const response = await fetch(`${JIMENG_BASE}${path}`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...headers },
    body,
  })
  const data = await response.json()
  if (data.ResponseMetadata?.Error) {
    throw new Error(data.ResponseMetadata.Error.Message)
  }
  return data.Result.job_id as string
}

// 查询任务状态
async function queryTaskStatus(jobId: string): Promise<{ status: TaskStatus; urls?: string[]; error?: string }> {
  const { jiMengAk, jiMengSk } = getConfig()
  const path = `/?Action=CVSync2AsyncGetResult&Version=2022-08-31&job_id=${jobId}`
  const headers = signRequest('GET', path, '', jiMengAk, jiMengSk)

  const response = await fetch(`${JIMENG_BASE}${path}`, {
    method: 'GET',
    headers,
  })
  const data = await response.json()
  const jobStatus = data.Result?.job_status

  if (jobStatus === 'done') {
    const urls = (data.Result?.images ?? []).map((img: { url: string }) => img.url)
    return { status: TaskStatus.SUCCESS, urls }
  } else if (jobStatus === 'failed') {
    return { status: TaskStatus.FAILED, error: data.Result?.message ?? '生成失败' }
  } else {
    return { status: TaskStatus.PROCESSING }
  }
}

// 启动轮询
function startPolling(localId: string, jobId: string, win: BrowserWindow) {
  let count = 0
  const timer = setInterval(async () => {
    count++
    if (count > MAX_POLL_COUNT) {
      clearInterval(timer)
      activePollers.delete(localId)
      win.webContents.send(IPC_CHANNELS.IMAGE_STATUS_UPDATE, {
        localId,
        patch: { status: TaskStatus.FAILED, errorMsg: '任务超时' }
      })
      return
    }

    try {
      const result = await queryTaskStatus(jobId)
      if (result.status !== TaskStatus.PROCESSING) {
        clearInterval(timer)
        activePollers.delete(localId)
      }
      win.webContents.send(IPC_CHANNELS.IMAGE_STATUS_UPDATE, {
        localId,
        patch: {
          status: result.status,
          ...(result.urls && { resultUrls: result.urls }),
          ...(result.error && { errorMsg: result.error }),
        }
      })
    } catch (err) {
      // 网络错误不停止轮询，继续重试
    }
  }, POLL_INTERVAL_MS)

  activePollers.set(localId, timer)
}

// 注册 IPC 处理器（在主进程 ready 后调用）
export function registerImageHandlers(win: BrowserWindow) {
  ipcMain.handle(IPC_CHANNELS.IMAGE_SUBMIT, async (_event, localId: string, params: ImageParams) => {
    const jobId = await submitImageTask(params)
    // 立即推送 PROCESSING 状态
    win.webContents.send(IPC_CHANNELS.IMAGE_STATUS_UPDATE, {
      localId,
      patch: { status: TaskStatus.PROCESSING, jobId }
    })
    startPolling(localId, jobId, win)
    return { jobId }
  })

  ipcMain.handle(IPC_CHANNELS.IMAGE_CANCEL, async (_event, localId: string) => {
    const timer = activePollers.get(localId)
    if (timer) {
      clearInterval(timer)
      activePollers.delete(localId)
    }
  })
}
```
  </action>
  <verify>
    <automated>ls src/main/imageHandler.ts</automated>
  </verify>
  <done>imageHandler.ts 存在，导出 registerImageHandlers，包含 submitImageTask、queryTaskStatus、startPolling 函数</done>
</task>

<task type="auto">
  <name>Task 2: 在主进程入口注册 imageHandler</name>
  <files>src/main/index.ts</files>
  <action>
在 src/main/index.ts 的 app.whenReady() 回调中，在 BrowserWindow 创建后调用 registerImageHandlers(win)：

```typescript
import { registerImageHandlers } from './imageHandler'

// 在 createWindow() 之后添加：
app.whenReady().then(() => {
  const win = createWindow()
  registerImageHandlers(win)
  // ... 其他已有初始化代码
})
```

如果 Phase 1 已有 src/main/index.ts，找到 BrowserWindow 创建的位置，在其后追加 registerImageHandlers(win) 调用。不要删除 Phase 1 已有的 IPC 注册代码（configStore、hmacSigner 相关）。
  </action>
  <verify>
    <automated>grep -n "registerImageHandlers" src/main/index.ts</automated>
  </verify>
  <done>src/main/index.ts 中包含 registerImageHandlers 调用</done>
</task>

</tasks>

<verification>
- src/main/imageHandler.ts 存在，导出 registerImageHandlers
- src/main/index.ts 中调用了 registerImageHandlers(win)
- imageHandler.ts 中 return_url: true 已设置（STATE.md 中记录的必要参数）
- 轮询使用 setInterval，activePollers Map 管理生命周期，cancel 时正确 clearInterval
</verification>

<success_criteria>
主进程可以接收 image:submit IPC 调用，提交即梦 API 任务，启动轮询，并通过 image:status-update 推送状态更新到渲染进程。多任务并行轮询互不干扰，cancel 可立即停止轮询。
</success_criteria>

<output>
完成后创建 .planning/phases/02-图片生成核心流程/02-02-SUMMARY.md
</output>
