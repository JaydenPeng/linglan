---
phase: 02-图片生成核心流程
plan: "04"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/views/TaskListView.vue
  - src/components/TaskCard.vue
autonomous: true
requirements:
  - TASK-01
  - TASK-02
  - TASK-03
  - TASK-04
  - RES-01

must_haves:
  truths:
    - "任务列表以卡片形式展示所有任务，最新任务排在最前"
    - "处理中任务卡片显示进度条动画和状态文字"
    - "失败任务卡片直接显示重试按钮"
    - "进行中任务卡片直接显示取消按钮"
    - "点击成功任务的缩略图打开全屏预览"
  artifacts:
    - path: "src/views/TaskListView.vue"
      provides: "任务列表页面，监听 IPC 状态更新"
      exports: ["default (Vue component)"]
    - path: "src/components/TaskCard.vue"
      provides: "单个任务卡片，含状态、操作按钮、缩略图"
      exports: ["default (Vue component)"]
  key_links:
    - from: "src/views/TaskListView.vue"
      to: "src/stores/taskStore.ts"
      via: "useTaskStore().tasks（响应式列表）"
      pattern: "useTaskStore|tasks"
    - from: "src/views/TaskListView.vue"
      to: "window.imageApi.onStatusUpdate"
      via: "监听主进程推送的状态更新，调用 taskStore.updateTask"
      pattern: "onStatusUpdate|updateTask"
    - from: "src/components/TaskCard.vue"
      to: "window.imageApi.cancel"
      via: "取消按钮点击时调用"
      pattern: "imageApi\\.cancel"
---

<objective>
实现任务列表页面和任务卡片组件，展示实时任务状态，支持重试和取消操作，成功任务显示缩略图。

Purpose: 用户提交任务后的主要视图，实时反映任务进度。
Output: src/views/TaskListView.vue + src/components/TaskCard.vue
</objective>

<execution_context>
@C:/Users/shuopeng/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shuopeng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-图片生成核心流程/02-CONTEXT.md
@.planning/phases/02-图片生成核心流程/02-01-SUMMARY.md

<interfaces>
From src/types/image.ts:
```typescript
export enum TaskStatus { PENDING, PROCESSING, SUCCESS, FAILED, CANCELLED }
export interface ImageTask { id, jobId?, type, status, params, createdAt, updatedAt, resultUrls?, errorMsg? }
```

From src/stores/taskStore.ts:
```typescript
tasks: Ref<ImageTask[]>
updateTask(id: string, patch: Partial<ImageTask>): void
cancelTask(id: string): void
```

window.imageApi:
- onStatusUpdate(cb): () => void  // 返回取消监听函数
- cancel(localId): Promise<void>
- submit(localId, params): Promise<{ jobId }>  // 重试时重新调用
</interfaces>

UI 决策（来自 CONTEXT.md，已锁定）：
- 卡片列表布局：提示词预览、状态标签、时间、缩略图（成功后）
- 处理中：卡片内嵌进度条动画 + 状态文字在右上角
- 失败：卡片内直接显示"重试"按钮
- 处理中：卡片内直接显示"取消"按钮
- 无长按菜单，操作直接可见
- 点击缩略图打开全屏预览（全屏预览在 Plan 05 实现，此处 emit 事件即可）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建任务卡片组件 TaskCard</name>
  <files>src/components/TaskCard.vue</files>
  <action>
创建 src/components/TaskCard.vue：

```vue
<template>
  <ion-card class="task-card">
    <!-- 状态标签（右上角） -->
    <div class="status-badge" :class="statusClass">{{ statusLabel }}</div>

    <ion-card-content>
      <!-- 提示词预览 -->
      <p class="prompt-preview">{{ task.params.prompt }}</p>

      <!-- 处理中：进度条动画 -->
      <ion-progress-bar v-if="isActive" type="indeterminate" />

      <!-- 成功：缩略图 -->
      <div v-if="task.status === TaskStatus.SUCCESS && task.resultUrls?.length" class="thumbnails">
        <img
          v-for="(url, i) in task.resultUrls"
          :key="i"
          :src="url"
          class="thumbnail"
          @click="$emit('preview', { task, index: i })"
        />
      </div>

      <!-- 失败：错误信息 -->
      <p v-if="task.status === TaskStatus.FAILED" class="error-msg">{{ task.errorMsg }}</p>

      <!-- 操作按钮行 -->
      <div class="actions">
        <span class="time">{{ timeAgo }}</span>
        <ion-button v-if="task.status === TaskStatus.FAILED" size="small" fill="outline" @click="$emit('retry', task)">
          重试
        </ion-button>
        <ion-button v-if="isActive" size="small" fill="outline" color="medium" @click="$emit('cancel', task)">
          取消
        </ion-button>
      </div>
    </ion-card-content>
  </ion-card>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { IonCard, IonCardContent, IonProgressBar, IonButton } from '@ionic/vue'
import { TaskStatus } from '../types/image'
import type { ImageTask } from '../types/image'

const props = defineProps<{ task: ImageTask }>()
defineEmits<{
  preview: [{ task: ImageTask; index: number }]
  retry: [task: ImageTask]
  cancel: [task: ImageTask]
}>()

const isActive = computed(() =>
  props.task.status === TaskStatus.PENDING || props.task.status === TaskStatus.PROCESSING
)

const statusLabel = computed(() => ({
  [TaskStatus.PENDING]: '已提交',
  [TaskStatus.PROCESSING]: '处理中',
  [TaskStatus.SUCCESS]: '成功',
  [TaskStatus.FAILED]: '失败',
  [TaskStatus.CANCELLED]: '已取消',
}[props.task.status]))

const statusClass = computed(() => ({
  [TaskStatus.PENDING]: 'status-pending',
  [TaskStatus.PROCESSING]: 'status-processing',
  [TaskStatus.SUCCESS]: 'status-success',
  [TaskStatus.FAILED]: 'status-failed',
  [TaskStatus.CANCELLED]: 'status-cancelled',
}[props.task.status]))

const timeAgo = computed(() => {
  const diff = Date.now() - props.task.createdAt
  if (diff < 60000) return '刚刚'
  if (diff < 3600000) return `${Math.floor(diff / 60000)} 分钟前`
  return `${Math.floor(diff / 3600000)} 小时前`
})
</script>

<style scoped>
.task-card { position: relative; margin: 8px 16px; border-radius: 12px; }
.status-badge {
  position: absolute; top: 8px; right: 8px;
  padding: 2px 8px; border-radius: 8px; font-size: 12px; font-weight: 600;
}
.status-pending { background: #e8f4fd; color: #1a73e8; }
.status-processing { background: #fff3e0; color: #f57c00; }
.status-success { background: #e8f5e9; color: #2e7d32; }
.status-failed { background: #fce4ec; color: #c62828; }
.status-cancelled { background: #f5f5f5; color: #757575; }
.prompt-preview { margin: 0 0 8px; font-size: 14px; overflow: hidden; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; }
.thumbnails { display: flex; gap: 8px; flex-wrap: wrap; margin: 8px 0; }
.thumbnail { width: 80px; height: 80px; object-fit: cover; border-radius: 8px; cursor: pointer; }
.error-msg { color: var(--ion-color-danger); font-size: 13px; margin: 4px 0; }
.actions { display: flex; align-items: center; justify-content: space-between; margin-top: 8px; }
.time { font-size: 12px; color: var(--ion-color-medium); }
</style>
```
  </action>
  <verify>
    <automated>ls src/components/TaskCard.vue</automated>
  </verify>
  <done>TaskCard.vue 存在，接受 task prop，emit preview/retry/cancel 事件，处理中显示进度条，失败显示重试按钮，成功显示缩略图</done>
</task>

<task type="auto">
  <name>Task 2: 创建任务列表页面 TaskListView</name>
  <files>src/views/TaskListView.vue</files>
  <action>
创建 src/views/TaskListView.vue：

```vue
<template>
  <ion-page>
    <ion-header>
      <ion-toolbar>
        <ion-title>任务列表</ion-title>
        <ion-buttons slot="end">
          <ion-button router-link="/create">
            <ion-icon :icon="addOutline" />
          </ion-button>
        </ion-buttons>
      </ion-toolbar>
    </ion-header>

    <ion-content>
      <div v-if="taskStore.tasks.length === 0" class="empty-state">
        <ion-icon :icon="imagesOutline" size="large" />
        <p>还没有任务，点击右上角开始创建</p>
      </div>

      <TaskCard
        v-for="task in taskStore.tasks"
        :key="task.id"
        :task="task"
        @preview="openPreview"
        @retry="handleRetry"
        @cancel="handleCancel"
      />
    </ion-content>

    <!-- 全屏预览（Plan 05 实现，此处预留插槽） -->
    <ImagePreviewModal
      v-if="previewState"
      :task="previewState.task"
      :initial-index="previewState.index"
      @close="previewState = null"
    />
  </ion-page>
</template>

<script setup lang="ts">
import { ref, onMounted, onUnmounted } from 'vue'
import {
  IonPage, IonHeader, IonToolbar, IonTitle, IonContent,
  IonButtons, IonButton, IonIcon
} from '@ionic/vue'
import { addOutline, imagesOutline } from 'ionicons/icons'
import { useTaskStore } from '../stores/taskStore'
import { TaskStatus } from '../types/image'
import type { ImageTask } from '../types/image'
import TaskCard from '../components/TaskCard.vue'
import ImagePreviewModal from '../components/ImagePreviewModal.vue'

const taskStore = useTaskStore()
const previewState = ref<{ task: ImageTask; index: number } | null>(null)

let unsubscribe: (() => void) | null = null

onMounted(() => {
  // 监听主进程推送的状态更新
  unsubscribe = window.imageApi.onStatusUpdate(({ localId, patch }) => {
    taskStore.updateTask(localId, patch as Partial<ImageTask>)
  })
})

onUnmounted(() => {
  unsubscribe?.()
})

function openPreview(payload: { task: ImageTask; index: number }) {
  previewState.value = payload
}

async function handleRetry(task: ImageTask) {
  const newId = crypto.randomUUID()
  taskStore.addTask({
    ...task,
    id: newId,
    jobId: undefined,
    status: TaskStatus.PENDING,
    resultUrls: undefined,
    errorMsg: undefined,
    createdAt: Date.now(),
    updatedAt: Date.now(),
  })
  try {
    await window.imageApi.submit(newId, task.params)
  } catch (err) {
    taskStore.updateTask(newId, { status: TaskStatus.FAILED, errorMsg: String(err) })
  }
}

async function handleCancel(task: ImageTask) {
  taskStore.cancelTask(task.id)
  await window.imageApi.cancel(task.id)
}
</script>

<style scoped>
.empty-state {
  display: flex; flex-direction: column; align-items: center;
  justify-content: center; height: 60vh; gap: 16px;
  color: var(--ion-color-medium);
}
</style>
```

注意：ImagePreviewModal 在 Plan 05 中实现。此处 import 它，如果文件不存在会导致编译错误。解决方案：先创建一个空的占位组件 src/components/ImagePreviewModal.vue（仅包含 `<template><div /></template><script setup>defineProps(['task','initialIndex']); defineEmits(['close'])</script>`），Plan 05 会替换它。
  </action>
  <verify>
    <automated>ls src/views/TaskListView.vue src/components/ImagePreviewModal.vue</automated>
  </verify>
  <done>TaskListView.vue 存在，onMounted 中注册 onStatusUpdate 监听，onUnmounted 中取消监听，支持重试（新建任务）和取消操作。ImagePreviewModal.vue 占位文件存在。</done>
</task>

</tasks>

<verification>
- TaskCard.vue 和 TaskListView.vue 均存在
- TaskListView 在 onMounted 中调用 window.imageApi.onStatusUpdate，在 onUnmounted 中取消监听
- 重试逻辑：创建新任务 ID，addTask 后调用 imageApi.submit
- 取消逻辑：先 taskStore.cancelTask（更新本地状态），再 imageApi.cancel（停止主进程轮询）
- ImagePreviewModal.vue 占位文件存在，不阻塞编译
</verification>

<success_criteria>
任务列表实时展示所有任务状态，处理中任务有进度条，失败任务有重试按钮，进行中任务有取消按钮，主进程推送的状态更新能正确反映到 UI。
</success_criteria>

<output>
完成后创建 .planning/phases/02-图片生成核心流程/02-04-SUMMARY.md
</output>
