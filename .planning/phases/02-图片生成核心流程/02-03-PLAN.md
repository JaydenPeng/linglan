---
phase: 02-图片生成核心流程
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/views/CreateView.vue
  - src/components/ImageParamsPanel.vue
autonomous: true
requirements:
  - IMG-01
  - IMG-02
  - IMG-03
  - IMG-04

must_haves:
  truths:
    - "用户可以在文生图 Tab 输入提示词并提交任务"
    - "用户可以切换到图生图 Tab，上传参考图片后提交任务"
    - "参数配置区域（宽高比、force_single）默认折叠，可展开配置"
    - "提交后自动跳转到任务列表页"
  artifacts:
    - path: "src/views/CreateView.vue"
      provides: "任务提交页面，含文生图/图生图 Tab"
      exports: ["default (Vue component)"]
    - path: "src/components/ImageParamsPanel.vue"
      provides: "可折叠参数配置面板"
      exports: ["default (Vue component)"]
  key_links:
    - from: "src/views/CreateView.vue"
      to: "src/stores/taskStore.ts"
      via: "useTaskStore().addTask()"
      pattern: "useTaskStore|addTask"
    - from: "src/views/CreateView.vue"
      to: "window.imageApi.submit"
      via: "IPC 调用提交任务"
      pattern: "imageApi\\.submit"
---

<objective>
实现任务提交界面：文生图/图生图 Tab 切换、提示词输入、参数配置、参考图上传，提交后写入 taskStore 并跳转任务列表。

Purpose: 用户发起任务的入口，覆盖 IMG-01 到 IMG-04 所有提交侧需求。
Output: src/views/CreateView.vue + src/components/ImageParamsPanel.vue
</objective>

<execution_context>
@C:/Users/shuopeng/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shuopeng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-图片生成核心流程/02-CONTEXT.md
@.planning/phases/02-图片生成核心流程/02-01-SUMMARY.md

<interfaces>
From src/types/image.ts:
```typescript
export enum TaskType { TEXT_TO_IMAGE = 'text_to_image', IMAGE_TO_IMAGE = 'image_to_image' }
export enum TaskStatus { PENDING = 'pending', ... }
export interface ImageParams { prompt, width?, height?, aspect_ratio?, force_single?, ref_image? }
export interface ImageTask { id, jobId?, type, status, params, createdAt, updatedAt, resultUrls?, errorMsg? }
```

From src/stores/taskStore.ts:
```typescript
export const useTaskStore = defineStore('task', () => ({
  tasks: Ref<ImageTask[]>,
  addTask: (task: ImageTask) => void,
  updateTask: (id: string, patch: Partial<ImageTask>) => void,
  cancelTask: (id: string) => void,
}))
```

window.imageApi.submit(localId: string, params: ImageParams): Promise<{ jobId: string }>
</interfaces>

UI 决策（来自 CONTEXT.md，已锁定）：
- 单页面 + Tab 切换，顶部 Tab 区分文生图/图生图
- 提示词输入框：多行自动拉伸
- 参数配置区域默认折叠，用户展开后配置
- 图生图：点击区域选图，选图后显示缩略图预览
- 提交后自动跳转到任务列表页
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建可折叠参数配置面板组件</name>
  <files>src/components/ImageParamsPanel.vue</files>
  <action>
创建 src/components/ImageParamsPanel.vue，使用 Ionic Vue 组件：

```vue
<template>
  <div class="params-panel">
    <ion-item button @click="expanded = !expanded" lines="none">
      <ion-label>参数配置</ion-label>
      <ion-icon :icon="expanded ? chevronUp : chevronDown" slot="end" />
    </ion-item>

    <div v-if="expanded" class="params-content">
      <!-- 宽高比选择 -->
      <ion-item>
        <ion-label>宽高比</ion-label>
        <ion-select v-model="localParams.aspect_ratio" interface="popover">
          <ion-select-option value="1:1">1:1 方形</ion-select-option>
          <ion-select-option value="16:9">16:9 横屏</ion-select-option>
          <ion-select-option value="9:16">9:16 竖屏</ion-select-option>
          <ion-select-option value="4:3">4:3</ion-select-option>
        </ion-select>
      </ion-item>

      <!-- 强制单图 -->
      <ion-item>
        <ion-label>强制单图输出</ion-label>
        <ion-toggle v-model="localParams.force_single" slot="end" />
      </ion-item>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, watch } from 'vue'
import { IonItem, IonLabel, IonSelect, IonSelectOption, IonToggle, IonIcon } from '@ionic/vue'
import { chevronDown, chevronUp } from 'ionicons/icons'
import type { ImageParams } from '../types/image'

const props = defineProps<{ modelValue: Partial<ImageParams> }>()
const emit = defineEmits<{ 'update:modelValue': [value: Partial<ImageParams>] }>()

const expanded = ref(false)
const localParams = ref<Partial<ImageParams>>({ aspect_ratio: '1:1', force_single: false, ...props.modelValue })

watch(localParams, (val) => emit('update:modelValue', val), { deep: true })
</script>
```
  </action>
  <verify>
    <automated>ls src/components/ImageParamsPanel.vue</automated>
  </verify>
  <done>ImageParamsPanel.vue 存在，接受 modelValue prop，emit update:modelValue，默认折叠</done>
</task>

<task type="auto">
  <name>Task 2: 创建任务提交页面 CreateView</name>
  <files>src/views/CreateView.vue</files>
  <action>
创建 src/views/CreateView.vue：

```vue
<template>
  <ion-page>
    <ion-header>
      <ion-toolbar>
        <ion-title>创建任务</ion-title>
      </ion-toolbar>
      <!-- Tab 切换 -->
      <ion-segment v-model="activeTab">
        <ion-segment-button value="text2img">文生图</ion-segment-button>
        <ion-segment-button value="img2img">图生图</ion-segment-button>
      </ion-segment>
    </ion-header>

    <ion-content class="ion-padding">
      <!-- 提示词输入 -->
      <ion-textarea
        v-model="prompt"
        placeholder="描述你想生成的图片..."
        :auto-grow="true"
        :rows="3"
        class="prompt-input"
      />

      <!-- 图生图：参考图上传 -->
      <div v-if="activeTab === 'img2img'" class="ref-image-area" @click="pickRefImage">
        <img v-if="refImagePreview" :src="refImagePreview" class="ref-preview" />
        <div v-else class="ref-placeholder">
          <ion-icon :icon="imageOutline" />
          <span>点击上传参考图片</span>
        </div>
      </div>

      <!-- 参数配置（折叠） -->
      <ImageParamsPanel v-model="imageParams" />

      <!-- 提交按钮 -->
      <ion-button expand="block" :disabled="!prompt.trim() || submitting" @click="handleSubmit">
        {{ submitting ? '提交中...' : '开始生成' }}
      </ion-button>
    </ion-content>
  </ion-page>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { useRouter } from 'vue-router'
import {
  IonPage, IonHeader, IonToolbar, IonTitle, IonContent,
  IonSegment, IonSegmentButton, IonTextarea, IonButton, IonIcon
} from '@ionic/vue'
import { imageOutline } from 'ionicons/icons'
import { useTaskStore } from '../stores/taskStore'
import { TaskStatus, TaskType } from '../types/image'
import type { ImageParams, ImageTask } from '../types/image'
import ImageParamsPanel from '../components/ImageParamsPanel.vue'

const router = useRouter()
const taskStore = useTaskStore()

const activeTab = ref<'text2img' | 'img2img'>('text2img')
const prompt = ref('')
const refImageBase64 = ref<string>('')
const refImagePreview = ref<string>('')
const imageParams = ref<Partial<ImageParams>>({ aspect_ratio: '1:1', force_single: false })
const submitting = ref(false)

async function pickRefImage() {
  // 使用 input[type=file] 选取图片，转 base64
  const input = document.createElement('input')
  input.type = 'file'
  input.accept = 'image/*'
  input.onchange = async (e) => {
    const file = (e.target as HTMLInputElement).files?.[0]
    if (!file) return
    const reader = new FileReader()
    reader.onload = (ev) => {
      const result = ev.target?.result as string
      refImagePreview.value = result
      // 去掉 data:image/xxx;base64, 前缀
      refImageBase64.value = result.split(',')[1]
    }
    reader.readAsDataURL(file)
  }
  input.click()
}

async function handleSubmit() {
  if (!prompt.value.trim() || submitting.value) return
  submitting.value = true

  const localId = crypto.randomUUID()
  const params: ImageParams = {
    prompt: prompt.value.trim(),
    ...imageParams.value,
    ...(activeTab.value === 'img2img' && refImageBase64.value
      ? { ref_image: refImageBase64.value }
      : {}),
  }

  const task: ImageTask = {
    id: localId,
    type: activeTab.value === 'text2img' ? TaskType.TEXT_TO_IMAGE : TaskType.IMAGE_TO_IMAGE,
    status: TaskStatus.PENDING,
    params,
    createdAt: Date.now(),
    updatedAt: Date.now(),
  }

  taskStore.addTask(task)

  try {
    await window.imageApi.submit(localId, params)
  } catch (err) {
    taskStore.updateTask(localId, { status: TaskStatus.FAILED, errorMsg: String(err) })
  } finally {
    submitting.value = false
  }

  // 提交后跳转任务列表
  router.push('/tasks')
}
</script>

<style scoped>
.prompt-input { margin-bottom: 16px; }
.ref-image-area {
  border: 2px dashed var(--ion-color-medium);
  border-radius: 12px;
  height: 160px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 16px;
  cursor: pointer;
  overflow: hidden;
}
.ref-preview { width: 100%; height: 100%; object-fit: cover; }
.ref-placeholder { display: flex; flex-direction: column; align-items: center; gap: 8px; color: var(--ion-color-medium); }
</style>
```
  </action>
  <verify>
    <automated>ls src/views/CreateView.vue</automated>
  </verify>
  <done>CreateView.vue 存在，包含 Tab 切换、提示词输入、参考图上传、参数面板、提交按钮，提交后调用 taskStore.addTask 和 window.imageApi.submit，跳转 /tasks</done>
</task>

</tasks>

<verification>
- src/views/CreateView.vue 和 src/components/ImageParamsPanel.vue 均存在
- CreateView 中 activeTab 控制文生图/图生图切换
- 图生图模式下显示参考图上传区域
- 参数面板默认折叠
- 提交时调用 taskStore.addTask 先写入本地状态，再调用 window.imageApi.submit
- 提交后 router.push('/tasks') 跳转
</verification>

<success_criteria>
用户可以在文生图/图生图两种模式下填写参数并提交任务，任务写入 taskStore，页面跳转到任务列表。
</success_criteria>

<output>
完成后创建 .planning/phases/02-图片生成核心流程/02-03-SUMMARY.md
</output>
