---
phase: 04-移动端适配
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/renderer/store/configStore.ts
autonomous: true
requirements:
  - MOB-01
  - MOB-02

must_haves:
  truths:
    - "configStore.refreshStatus 在 Capacitor 环境下通过 storageService 读取密钥状态，不调用 window.electron"
    - "configStore.refreshStatus 在 Electron 环境下继续走 window.electron IPC，行为不变"
  artifacts:
    - path: "src/renderer/store/configStore.ts"
      provides: "跨平台 configStore，Capacitor 路径走 storageService"
      exports: ["useConfigStore"]
  key_links:
    - from: "src/renderer/store/configStore.ts"
      to: "src/renderer/bridge/platform.ts"
      via: "isCapacitor() 环境检测决定调用路径"
      pattern: "isCapacitor"
    - from: "src/renderer/store/configStore.ts"
      to: "src/services/storageService.ts"
      via: "Capacitor 路径：storageService.getItem 读取密钥存在性"
      pattern: "storageService\\.getItem"
---

<objective>
将 Pinia configStore 接入共享服务层：Capacitor 环境下通过 storageService 读取密钥状态，Electron 环境继续走 IPC。

Purpose: CONTEXT.md 锁定决策要求"渲染进程通过环境检测决定调用路径"，configStore 是密钥状态的唯一消费者，必须实现双路径。
Output: 修改后的 src/renderer/store/configStore.ts，支持 Electron/Capacitor 双路径。
</objective>

<execution_context>
@C:/Users/shuopeng/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shuopeng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-移动端适配/04-CONTEXT.md
@.planning/phases/04-移动端适配/04-01-SUMMARY.md

<interfaces>
<!-- 04-01 产出的接口，executor 直接使用 -->

From src/renderer/bridge/platform.ts:
```typescript
export function isElectron(): boolean
export function isCapacitor(): boolean
```

From src/services/storageService.ts:
```typescript
export function getItem(key: string): Promise<string | null>
export function setItem(key: string, value: string): Promise<void>
export function removeItem(key: string): Promise<void>
```

<!-- 密钥存储 key 约定（与 storageService 实现一致）：-->
<!-- jimeng AK: 'jimeng_ak', jimeng SK: 'jimeng_sk' -->
<!-- kling AK: 'kling_ak', kling SK: 'kling_sk' -->

<!-- 现有 configStore（Electron-only）：-->
From src/renderer/store/configStore.ts:
```typescript
// 当前实现：仅 Electron 路径
async function refreshStatus(): Promise<void> {
  const result = await window.electron.getConfigStatus()
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: configStore 双路径接线</name>
  <files>src/renderer/store/configStore.ts</files>
  <action>
    修改 src/renderer/store/configStore.ts，在 refreshStatus 中加入环境检测分支：

    ```typescript
    import { defineStore } from 'pinia'
    import { ref } from 'vue'
    import { isCapacitor } from '../bridge/platform'
    import { getItem } from '../../services/storageService'

    export const useConfigStore = defineStore('config', () => {
      const jimengConfigured = ref(false)
      const klingConfigured = ref(false)
      const loading = ref(false)

      async function refreshStatus(): Promise<void> {
        loading.value = true
        try {
          if (isCapacitor()) {
            // Capacitor 路径：直接读取 storageService（Preferences）
            const jimengAk = await getItem('jimeng_ak')
            const klingAk = await getItem('kling_ak')
            jimengConfigured.value = !!jimengAk
            klingConfigured.value = !!klingAk
          } else {
            // Electron 路径：IPC（保持原有行为不变）
            const result = await window.electron.getConfigStatus()
            if (result.success && result.data) {
              jimengConfigured.value = result.data.jimengConfigured
              klingConfigured.value = result.data.klingConfigured
            }
          }
        } finally {
          loading.value = false
        }
      }

      return { jimengConfigured, klingConfigured, loading, refreshStatus }
    })
    ```

    注意：
    - 密钥 key 名称（'jimeng_ak'、'kling_ak'）必须与 storageService 实现中 setItem 使用的 key 一致
    - 如果 04-01 的 storageService 使用了不同的 key 命名，以 04-01-SUMMARY.md 中记录的为准
    - Electron 路径代码不做任何改动，确保桌面端行为不受影响
  </action>
  <verify>
    <automated>npm run test -- src/renderer/store</automated>
  </verify>
  <done>configStore.ts 包含 isCapacitor() 分支，Capacitor 路径调用 storageService.getItem，Electron 路径调用 window.electron.getConfigStatus，现有 store 测试（如有）全部通过</done>
</task>

</tasks>

<verification>
- `npm run test -- src/renderer/store` 通过
- `npx tsc --noEmit` 无 TypeScript 编译错误
- configStore.ts 中同时存在 isCapacitor() 和 window.electron 两个分支
</verification>

<success_criteria>
- configStore.ts 导入 isCapacitor 和 storageService.getItem
- refreshStatus 在 Capacitor 环境下不调用 window.electron
- refreshStatus 在 Electron 环境下行为与修改前完全一致
- TypeScript 编译无错误
</success_criteria>

<output>
完成后创建 `.planning/phases/04-移动端适配/04-03-SUMMARY.md`
</output>
