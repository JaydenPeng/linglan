---
phase: 04-移动端适配
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/bridge/platform.ts
  - src/renderer/bridge/platform.test.ts
  - src/services/storageService.ts
  - src/services/storageService.test.ts
  - src/services/jimengService.ts
  - src/services/jimengService.test.ts
  - src/services/klingService.ts
  - src/services/klingService.test.ts
  - src/services/downloadService.ts
  - src/services/downloadService.test.ts
autonomous: true
requirements:
  - MOB-01
  - MOB-02

must_haves:
  truths:
    - "环境检测函数能正确区分 Electron 和 Capacitor 运行环境"
    - "storageService 在 Capacitor 环境下使用 Preferences 存取密钥"
    - "jimengService 能构建正确的 HMAC-SHA256 签名 Header"
    - "klingService 能构建正确的 JWT Bearer Header"
    - "downloadService 在 web 路径下能触发浏览器下载"
  artifacts:
    - path: "src/renderer/bridge/platform.ts"
      provides: "isElectron / isCapacitor 环境检测"
      exports: ["isElectron", "isCapacitor"]
    - path: "src/services/storageService.ts"
      provides: "跨平台密钥存取（Capacitor Preferences / electron IPC）"
      exports: ["getItem", "setItem", "removeItem"]
    - path: "src/services/jimengService.ts"
      provides: "即梦 API 调用（含 HMAC 签名，@capacitor-community/http）"
      exports: ["submitImageTask", "pollImageTask"]
    - path: "src/services/klingService.ts"
      provides: "可灵 API 调用（含 JWT，@capacitor-community/http）"
      exports: ["submitVideoTask", "pollVideoTask"]
    - path: "src/services/downloadService.ts"
      provides: "跨平台文件下载（web anchor / Capacitor Filesystem）"
      exports: ["downloadFile"]
  key_links:
    - from: "src/services/jimengService.ts"
      to: "src/main/api/hmacSigner.ts 逻辑"
      via: "crypto-js 复现 HMAC-SHA256 签名（浏览器兼容）"
      pattern: "CryptoJS.HmacSHA256"
    - from: "src/services/klingService.ts"
      to: "src/main/auth/jwtSigner.ts 逻辑"
      via: "crypto-js 复现 JWT HS256（浏览器兼容）"
      pattern: "CryptoJS.HmacSHA256"
    - from: "src/services/storageService.ts"
      to: "@capacitor/preferences"
      via: "Preferences.get / Preferences.set"
      pattern: "Preferences\\.(get|set|remove)"
    - from: "src/renderer/store/configStore.ts"
      to: "src/services/storageService.ts"
      via: "04-03 接线：Capacitor 环境下 refreshStatus 调用 storageService.getItem"
      pattern: "isCapacitor.*storageService"
---

<objective>
建立 Capacitor 移动端共享服务层：环境检测、跨平台密钥存储、即梦/可灵 API 直调服务、文件下载服务。同时为每个服务创建 Wave 0 测试文件（先写失败测试，再实现）。

Purpose: 移动端无 Electron 主进程，渲染进程需要直接调用 API。共享服务层封装平台差异，Pinia store 通过环境检测选择调用路径。
Output: src/services/ 下 5 个服务文件 + 对应测试文件，src/renderer/bridge/platform.ts 环境检测工具。
</objective>

<execution_context>
@C:/Users/shuopeng/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shuopeng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-移动端适配/04-CONTEXT.md
@.planning/phases/04-移动端适配/04-RESEARCH.md

<interfaces>
<!-- 现有代码接口，executor 直接使用，无需探索代码库 -->

From src/main/api/hmacSigner.ts:
```typescript
// 即梦 HMAC-SHA256 签名逻辑（Node.js crypto）
// jimengService.ts 需用 crypto-js 复现相同逻辑（浏览器兼容）
export function signRequest(
  method: string,
  path: string,      // 含 query，如 '/?Action=CVSync2AsyncSubmitTask&Version=2022-08-31'
  body: string,
  ak: string,
  sk: string
): Record<string, string>
// 返回 { 'X-Date', 'Authorization', 'Host', 'Content-Type'? }
// HOST = 'visual.volcengineapi.com', SERVICE = 'cv', REGION = 'cn-north-1'
```

From src/main/auth/jwtSigner.ts:
```typescript
// 可灵 JWT HS256（Node.js crypto）
// klingService.ts 需用 crypto-js 复现相同逻辑
export async function getKlingToken(ak: string, sk: string): Promise<string>
// JWT payload: { iss: ak, exp: now+1800, nbf: now-5 }
// 缓存逻辑：tokenExpiry - now > 60 时复用
```

From src/shared/types.ts:
```typescript
export interface ConfigStatus {
  jimengConfigured: boolean
  klingConfigured: boolean
}
export interface SaveConfigPayload {
  jimengAk?: string; jimengSk?: string
  klingAk?: string; klingSk?: string
}
export interface IpcResult<T = void> {
  success: boolean; data?: T; error?: string
}
```

From src/renderer/bridge/ipcClient.ts:
```typescript
// window.electron 仅在 Electron 环境存在
declare global {
  interface Window {
    electron: {
      getConfigStatus(): Promise<IpcResult<ConfigStatus>>
      saveConfig(payload: SaveConfigPayload): Promise<IpcResult>
      clearConfig(provider: 'jimeng' | 'kling'): Promise<IpcResult>
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: 环境检测 + storageService（Wave 0 测试先行）</name>
  <files>
    src/renderer/bridge/platform.ts,
    src/renderer/bridge/platform.test.ts,
    src/services/storageService.ts,
    src/services/storageService.test.ts
  </files>
  <behavior>
    platform.ts:
    - isElectron(): boolean — 检测 window.electron 是否存在
    - isCapacitor(): boolean — 检测 window.Capacitor?.isNativePlatform() 是否为 true

    storageService.ts:
    - getItem(key: string): Promise&lt;string | null&gt; — Capacitor 环境用 Preferences.get，Electron 环境通过 window.electron IPC 读取（或 localStorage fallback）
    - setItem(key: string, value: string): Promise&lt;void&gt;
    - removeItem(key: string): Promise&lt;void&gt;

    测试用例（platform.test.ts）:
    - isElectron() 在 window.electron 存在时返回 true
    - isElectron() 在 window.electron 不存在时返回 false
    - isCapacitor() 在 window.Capacitor.isNativePlatform() 为 true 时返回 true

    测试用例（storageService.test.ts）:
    - mock @capacitor/preferences，setItem 调用 Preferences.set
    - mock @capacitor/preferences，getItem 调用 Preferences.get 并返回值
    - removeItem 调用 Preferences.remove
  </behavior>
  <action>
    1. 先写 platform.test.ts 和 storageService.test.ts（失败测试）
    2. 运行 `npm run test -- src/renderer/bridge/platform` 确认 RED
    3. 实现 platform.ts：
       ```typescript
       export function isElectron(): boolean {
         return typeof window !== 'undefined' && !!window.electron
       }
       export function isCapacitor(): boolean {
         return typeof window !== 'undefined' &&
           !!(window as any).Capacitor?.isNativePlatform?.()
       }
       ```
    4. 实现 storageService.ts：
       - import { Preferences } from '@capacitor/preferences'
       - getItem/setItem/removeItem 直接调用 Preferences API（移动端原生存储）
       - 注意：storageService 专为 Capacitor 路径设计，Electron 路径继续走 IPC
    5. 运行测试确认 GREEN
  </action>
  <verify>
    <automated>npm run test -- src/renderer/bridge/platform src/services/storageService</automated>
  </verify>
  <done>platform.ts 导出 isElectron/isCapacitor，storageService.ts 导出 getItem/setItem/removeItem，两个测试文件全部通过</done>
</task>

<task type="tdd">
  <name>Task 2: jimengService + klingService + downloadService（Wave 0 测试先行）</name>
  <files>
    src/services/jimengService.ts,
    src/services/jimengService.test.ts,
    src/services/klingService.ts,
    src/services/klingService.test.ts,
    src/services/downloadService.ts,
    src/services/downloadService.test.ts
  </files>
  <behavior>
    jimengService.ts:
    - buildJimengHeaders(method, path, body, ak, sk): Record&lt;string, string&gt; — 用 crypto-js 复现 hmacSigner.ts 的 HMAC-SHA256 签名逻辑
    - submitImageTask(params, ak, sk): Promise&lt;string&gt; — 用 @capacitor-community/http 发送 POST，返回 task_id
    - pollImageTask(taskId, ak, sk): Promise&lt;ImageTask&gt; — 用 @capacitor-community/http 发送 GET

    klingService.ts:
    - buildKlingToken(ak, sk): string — 用 crypto-js 复现 jwtSigner.ts 的 JWT HS256 逻辑（含缓存）
    - submitVideoTask(params, ak, sk): Promise&lt;string&gt;
    - pollVideoTask(taskId, ak, sk): Promise&lt;VideoTask&gt;

    downloadService.ts:
    - downloadFile(url: string, filename: string): Promise&lt;void&gt;
      - Capacitor 环境：用 @capacitor/filesystem 写入 Documents 目录
      - Web/Electron 环境：创建 &lt;a&gt; 标签触发浏览器下载

    测试用例（jimengService.test.ts）:
    - buildJimengHeaders 返回包含 'X-Date' 和 'Authorization' 的对象
    - Authorization 以 'HMAC-SHA256 Credential=' 开头

    测试用例（klingService.test.ts）:
    - buildKlingToken 返回三段式 JWT 字符串（header.payload.sig）
    - 连续调用两次，第二次返回缓存 token（相同字符串）

    测试用例（downloadService.test.ts）:
    - web 路径：mock document.createElement，验证创建了 &lt;a&gt; 元素并调用 click()
  </behavior>
  <action>
    1. 先写三个测试文件（失败测试），运行确认 RED
    2. 实现 jimengService.ts：
       - 用 CryptoJS.HmacSHA256 复现签名逻辑（与 hmacSigner.ts 算法一致）
       - HOST = 'visual.volcengineapi.com', SERVICE = 'cv', REGION = 'cn-north-1'
       - HTTP 请求使用 @capacitor-community/http 的 CapacitorHttp.request()
       - 请求格式：{ method, url, headers, data }
    3. 实现 klingService.ts：
       - 用 CryptoJS.HmacSHA256 复现 JWT 签名（base64url 编码）
       - JWT payload: { iss: ak, exp: now+1800, nbf: now-5 }
       - 缓存逻辑：模块级变量 cachedToken / tokenExpiry
       - HTTP 请求使用 CapacitorHttp.request()
    4. 实现 downloadService.ts：
       - isCapacitor() 为 true：import { Filesystem, Directory } from '@capacitor/filesystem'，fetch URL 转 base64，写入 Directory.Documents
       - 否则：document.createElement('a') + URL.createObjectURL + click()
    5. 运行测试确认 GREEN
  </action>
  <verify>
    <automated>npm run test -- src/services/jimengService src/services/klingService src/services/downloadService</automated>
  </verify>
  <done>三个服务文件实现完毕，测试全部通过。jimengService 的 Authorization header 格式与 hmacSigner.ts 输出一致，klingService 的 JWT 格式与 jwtSigner.ts 输出一致</done>
</task>

</tasks>

<verification>
运行完整测试套件：`npm run test:unit`

验证所有 5 个新文件的测试通过：
- src/renderer/bridge/platform.test.ts
- src/services/storageService.test.ts
- src/services/jimengService.test.ts
- src/services/klingService.test.ts
- src/services/downloadService.test.ts

验证 TypeScript 编译无错误：`npx tsc --noEmit`
</verification>

<success_criteria>
- src/services/ 目录下 5 个服务文件存在且导出正确接口
- src/renderer/bridge/platform.ts 导出 isElectron / isCapacitor
- 所有单元测试通过（`npm run test:unit` 绿色）
- jimengService 签名逻辑与主进程 hmacSigner.ts 算法一致
- klingService JWT 逻辑与主进程 jwtSigner.ts 算法一致
- 无 TypeScript 编译错误
</success_criteria>

<output>
完成后创建 `.planning/phases/04-移动端适配/04-01-SUMMARY.md`
</output>
